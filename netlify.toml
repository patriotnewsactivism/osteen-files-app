[build]
  command = "npm run build"
  publish = "dist"         # Ã¢â€ Â not "build"
  # base = "wtp-app"       # Ã¢â€ Â only if your app lives in a subfolder
t", ".next", etc., depending on framework

